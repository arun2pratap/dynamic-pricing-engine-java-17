package org.sample.pricing.ip;

import static org.junit.jupiter.api.Assertions.assertEquals;

import org.junit.jupiter.api.Test;
import org.mockito.Mock;
import org.sample.pricing.client.SubscriberDataService;

import java.util.HashMap;
import java.util.Map;

public class DynamicPricingStrategyTest {

    @Mock
    private DynamicPricingStrategy dynamicPricingStrategy;
    @Test
    public void testCalculatePrice() {
//        double basePrice = 100.0;
//        double expectedFinalPrice = 120.0;
//
//        Map<String, Object> additionalData = new HashMap<>();
//
//        double finalPrice = dynamicPricingStrategy.calculatePrice(basePrice, additionalData);
//        assertEquals(expectedFinalPrice, finalPrice, 0.001);
    }
}
